name: CI
on:
  workflow_dispatch:
  # auto-triggers disabled due to billing lock; re-enable when resolved
  # push:
  # pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run lint --if-present
